---
# `pip install ansible` already bundles this; `pip install ansible-core` does not.
#   ansible-galaxy collection install -r requirements.yml
collections:
  - name: ansible.posix
    version: ">=1.5.0"
